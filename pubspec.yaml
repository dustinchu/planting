name: tree
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  provider: 4.0.4
  after_layout: 1.0.7+2 #第一禎加載
  font_awesome_flutter: ^8.8.1 # icon
  flutter_spinkit: ^4.1.2+1 # 隨時間變化而變化的加載指示器集合
  google_fonts: 0.3.10
flutter:
  uses-material-design: true
  assets:
    - assets/images/