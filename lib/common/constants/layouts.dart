/// constant for Layout category
enum pCategoriesLayout {
  card,
  sideMenu,
  column,
  subCategories,
  animation,
  grid
}

/// For Blog type constant
enum pBlogLayout {
  simpleType,
  fullSizeImageType,
  halfSizeImageType,
  oneQuarterImageType
}
